apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: prod
resources:
  - ../../base
patches:
  - path: configmap.yaml
    target: { kind: ConfigMap, name: atelier-app-config }
  - path: deployment-patch.yaml
    target: { kind: Deployment, name: atelier-app }
